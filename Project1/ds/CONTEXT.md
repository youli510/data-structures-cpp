# 当前学习状态

## 进度
- [x] 栈（3种实现 + 括号匹配 + 撤销/重做 + 函数调用栈）
- [x] 队列（3种实现 + BFS + 消息队列）
- [x] 哈希表（拉链法 + rehash）
- [x] 树（前中后序，递归6种+迭代6种，层序BFS）
- [x] 堆（最小堆/最大堆 + 上浮/下沉 + TopK + 堆排序 + 原地堆排序）
- [ ] 图 ← 当前（邻接表链表版 + vector版 + 邻接矩阵：BFS/DFS 全部 ✅；LeetCode 200 岛屿数量 AC；待做 A*）
## 下一步
1. 图（邻接表/邻接矩阵 + DFS/BFS + A*）
2. 排序（快排/归并 能手写）
3. 二分查找
4. C++ 深入（智能指针/移动语义/虚函数表）

## 关键文件
- ds/linear/ → 栈/队列/链表所有代码
- ds/hash/ → 哈希表
- ds/tree/bst_traversal.cpp → 树遍历（已完成6种）
- ds/LeetCode_Roadmap.docx → 刷题清单
- ds/graph/graph.cpp → 图的邻接表（链表版）

## 最近更新
2026-07-30 图：邻接表（链表版 + vector版）+ BFS + DFS（递归/迭代）全部完成；LeetCode 200 岛屿数量 AC

## 待办
- 图下一步：A* 寻路（优先队列 + 启发函数）
- 之后：排序（快排/归并/堆排 手写）
- heap.cpp 类名 Minheap 是最小堆逻辑（正确）